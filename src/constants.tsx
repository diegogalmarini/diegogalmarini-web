import React from 'react';

// Google Partner Icon placeholder - will be replaced with actual icon
export const GooglePartnerIcon: React.FC = () => (
  <div className="w-6 h-6 bg-[var(--primary-color)] rounded flex items-center justify-center">
    <span className="text-white text-xs font-bold">G</span>
  </div>
);

// TODO: Import all constants from original project in later phases
// This is a basic setup to prevent compilation errors